import SectionTitle from '../components/common/SectionTitle'

export default function AboutPage() {
  return (
    <section>
      <SectionTitle title="About" subtitle="About page placeholder." />
      <p>Brand story, mission and pizza nerd context go here.</p>
    </section>
  )
}