import SectionTitle from '../components/common/SectionTitle'

export default function ConfirmationPage() {
  return (
    <section>
      <SectionTitle title="Confirmation" subtitle="Order confirmation placeholder." />
      <p>Thank you page for a completed order.</p>
    </section>
  )
}